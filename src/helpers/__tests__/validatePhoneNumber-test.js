// @flow strict

import validatePhoneNumber from '../validatePhoneNumber';

describe('validatePhoneNumber', () => {
  it('should return true when phone number valid', () => {
    // Write some tests here
  });

  it('should return false when phone number not valid', () => {
    // Write some tests here
  });
});
